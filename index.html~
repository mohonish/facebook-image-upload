<!doctype html>
<html ng-app="plunker">
    
    <head>
        <script src="angular.min.js">
        </script>
        <script src="ui.bootstrap.js">
        </script>
        <link href="//netdna.bootstrapcdn.com/twitter-bootstrap/2.3.1/css/bootstrap-combined.min.css" rel="stylesheet">
        <script src="ngFacebook.js"></script>
        <script src="app.js"></script>
    </head>
    
    <body ng-controller="DemoCtrl">
        <div class="container">
            <h4>
                {{welcomeMsg}}
            </h4>
            <button type="button" ng-click="login()" ng-hide="isLoggedIn" class="btn btn-default navbar-btn">
                Login
            </button>
            <button type="button" ng-click="logout()" ng-hide="!isLoggedIn" class="btn btn-default navbar-btn">
                Logout
            </button>
            <button type="button" ng-click="test()" ng-hide="!isLoggedIn" class="btn btn-default navbar-btn">
                Test
            </button>
            <input type="file" name="file" onchange="angular.element(this).scope().uploadFile(this.files)"/>
        </div>
        <div id="fb-root">
        </div>
    </body>

</html>
